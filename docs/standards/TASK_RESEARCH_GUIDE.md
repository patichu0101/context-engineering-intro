# 📚 Task-Specific Research Protocol

Research drives good implementation. Follow this before writing code:

## 👁 High-Level Queries (Patterns, Architecture)

```bash
archon:perform_rag_query(query="microservices vs monolith", match_count=5)
```

## 🔧 Low-Level Queries (Implementation)

```bash
archon:perform_rag_query(query="Express JWT middleware", match_count=3)
archon:search_code_examples(query="custom hook data fetching", match_count=3)
```

## 🚡 Security, Setup, Debugging

- "OAuth 2.0 PKCE flow implementation"
- "PostgreSQL pooling best practices"
- "TypeScript generic inference error"

## ✅ Checklist Before Coding

- [ ] RAG performed
- [ ] Code examples reviewed
- [ ] Antipatterns and caveats noted
